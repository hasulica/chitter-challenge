<!DOCTYPE html>
<html>
  <head>
    <title>Bookmark Manager</title>
  </head>

  <body>
    <% if flash[:errors] %>
      <div id='errors'><%= flash[:errors] %></div>
    <% end %>
    <% if current_user %>
      Welcome, <%= current_user.name %>!
    <% end %>
    <%= yield %>
  </body>
</html>
